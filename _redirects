# Netlify redirects file
# This prevents 404 errors when accessing pages directly

# Redirect API calls to show a friendly message instead of 404
/api/*  /index.html  200

# Handle admin routes
/admin/*  /admin/index.html  200

# Catch-all for other routes - redirect to main app
/*  /index.html  200
